language: python

dist: xenial

#arch:
#  - amd64  # Default
#  - arm64  # No Python images yet on xenial

python:
  - 3.1
  - 3.8
  - 3.10
  - "3.1"
  - "3.8"
  - "3.10"

install:
 - pip install -U tqdm

script:
  - echo "$DIST"
  - lsb_release -a
  - python --version
  - python ./test.py

