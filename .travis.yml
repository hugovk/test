language: python

#arch:
#  - amd64  # Default
#  - arm64  # No Python images yet on xenial

python:
  - 'pypy3'
  - '3.8'
  - '3.7'
  - '3.6'
  - '3.5'
  - 'nightly'
  - '3.9-dev'
  - '3.8-dev'
  - '3.7-dev'
  - '3.6-dev'

install:
 - pip install -U pip
 - pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple ujson==2.0.4.dev47 --no-cache


script:
  - echo "$DIST"
  - lsb_release -a
  - python --version
  - python ./smoke.py
